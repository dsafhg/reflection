When to commit?
One commit per logical change

What happens when you initialize a repository? Why do you need to do it?
It creates a .git file which tracks the changes in files in the directory. Nothing is commited 